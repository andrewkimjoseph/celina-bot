{
  "name": "celina-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram bot for Celina API read tools on Celo mainnet",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "sync-aliases": "node --import tsx scripts/generate-aliases.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@andrewkimjoseph/celina-sdk": "0.25.24",
    "@cloudflare/workers-types": "^5.20260815.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.0"
  }
}
